//styles
import classes from './Home.module.css';

const Home = () =>
{
    return (
        <section className={ classes.starting }>
            <h1>Welcome Home!</h1>
        </section>
    );
};

export default Home;